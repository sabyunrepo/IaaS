<claude-mem-context>
# Recent Activity

<!-- This section is auto-generated by claude-mem. Edit content outside the tags. -->

### Feb 3, 2026

| ID | Time | T | Title | Read |
|----|------|---|-------|------|
| #609 | 1:53 AM | 🟣 | Workflow Retry Policies and Document Size Limits Implemented | ~667 |
| #608 | 1:52 AM | 🟣 | Workflow Retry Policies and Document Size Limits Implemented (P15) | ~672 |
| #510 | 1:06 AM | 🔵 | Interview Generation Workflow Implementation Complete | ~835 |

### Feb 4, 2026

| ID | Time | T | Title | Read |
|----|------|---|-------|------|
| #1112 | 3:13 AM | 🔵 | Comprehensive CLAUDE.md Documentation Files Created Throughout Codebase | ~614 |

### Feb 6, 2026

| ID | Time | T | Title | Read |
|----|------|---|-------|------|
| #2596 | 5:27 AM | 🔵 | Workflow Orchestrates 4-Phase Pipeline with v2 Intel/Analysis Generation | ~884 |

### Feb 8, 2026

| ID | Time | T | Title | Read |
|----|------|---|-------|------|
| #4050 | 6:20 AM | 🔵 | Backend Performance Audit Completed for IaaS Application | ~552 |
</claude-mem-context>

# backend/app/workflows/

Temporal 워크플로우 오케스트레이션.

## 구조

| 파일 | 역할 |
|------|------|
| `interview_workflow.py` | 메인 워크플로우 — 4-Phase 파이프라인 |
| `utils.py` | `run_llm_with_prompt_config_heartbeat()` (Langfuse-first), `run_llm_with_heartbeat()` (레거시) |
| `activities/` | 16개 Activity 함수 (아래 참조) |

## 워크플로우 파이프라인

Phase 0: JD 분석 + 문서 파싱 → Phase 1: 코드/LinkedIn 분석 (병렬)
→ Phase 2: 질문 생성 (Knowledge Graph 우선) → Phase 3: Intel/Deep Analysis/Decision
→ Phase 4: Finalization (면접 스크립트 조립)

## LLM 호출 패턴 (Langfuse-First)

모든 LLM Activity에서 `get_prompt_with_config()` + `run_llm_with_prompt_config_heartbeat()` 패턴 사용:
- Langfuse 우선: Langfuse UI에서 모델/온도/토큰 변경 시 코드 배포 없이 즉시 반영
- YAML fallback: Langfuse 미설정 시 YAML + `ACTIVITY_MODEL_CONFIG` 자동 fallback
- `CachedLLMService.run_with_prompt_config()`: `PromptWithConfig`에서 설정 추출
- Phase 4 v2 Activity: KG/VectorStore 시맨틱 컨텍스트를 `kg_context` 프롬프트 변수로 주입
- `LLM_CACHE_ENABLED=false` 시 캐시 완전 바이패스

## 질문 배분 시스템 (PR #86)

경험 레벨별 퍼센트 기반 질문 배분:
- `question_distribution` dict로 카테고리별 비율 정의
- `max_questions` → 레벨별 기본값 (신입 12, 주니어 15, 미들 18, 시니어 20, CTO 22)
- UUID 기반 질문 ID 생성 (`uuid.uuid5`)

## 카테고리 가중치 (PR #86-87)

`interview_workflow.py` line 402: 경험 레벨별 가중치 적용
- `input_data.get("experience_level", "미들")` — flat dict 직접 접근
- 시니어: role_fit 15%, technical 20%, execution 25%, communication 20%, risk 20%
- 미들: role_fit 25%, technical 20%, execution 20%, communication 20%, risk 15%

## output_language 전파 (PR #91)

7개 Activity에 `output_language` 주입:
- `input_data.get("output_language", "ko")`로 프롬프트에 전달
- YAML 프롬프트에 `{output_language}` 플레이스홀더 추가
- `llm_config.py` 헬퍼로 모델/언어 설정 통합

## 레이더 차트 축 변경 (PR #85)

`analysis_generation.py`: 5번 축 risk → code_quality
- 코드 품질 점수: test_coverage(40%) + documentation(30%) + complexity(30%)
- `_extract_risk_flags()`는 유지 — DeepAnalysis.risk_flags에 계속 저장

## GLM→Kimi 모델 동기화 (PR #93)

- `upload_prompts_to_langfuse.py`: GLM 상수 제거, `llm_config.py` 단일 소스 import
- `code_analysis.py`: `GLM_MODEL` → `KIMI_CODER_MODEL` (llm_config.py에서 import)
- `cached_llm.py`: Langfuse span 경고 수정 (active trace 분기)
- `prompts/__init__.py`: `clear_langfuse_prompt_cache()` 편의 함수 추가

## i18n + Langfuse + 데이터 파이프라인 수정 (5단계)

1. **Langfuse SDK v3 호환**: `client.generation()` → `client.trace().generation()` (cached_llm.py)
2. **Finalization i18n**: `finalization.yaml` 양쪽 프롬프트에 `{{output_language}}` 디렉티브 추가
3. **Rule-based i18n**: 신규 `i18n_labels.py` + intel/analysis/decision fallback 한국어 → `_t()` 치환
4. **code_analysis 메타데이터**: `final_script.metadata`에 `has_code_analysis`, `code_analysis_tech_stack` 등 추가
5. **LinkedIn 필드 방어**: `full_name`/`name`, `experiences`/`experience`, `avatar_url`/`avatar` OR 패턴