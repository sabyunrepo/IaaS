<claude-mem-context>
# Recent Activity

<!-- This section is auto-generated by claude-mem. Edit content outside the tags. -->

### Feb 8, 2026

| ID | Time | T | Title | Read |
|----|------|---|-------|------|
| #4052 | 6:21 AM | 🔵 | OWASP Security Audit Identified 9 Vulnerabilities in FastAPI Backend | ~781 |
| #4038 | 6:06 AM | 🔵 | Git operations use PyDriller Repository class, not direct git subprocess calls | ~591 |
| #4022 | 5:58 AM | 🔵 | Backend Dockerfile multi-stage build examined | ~601 |
| #3942 | 5:07 AM | 🟣 | LLM skill matching succeeded with 61% overall match using Kimi model | ~526 |
| #3940 | 5:01 AM | 🔵 | New job 437a5cb9 started immediately after worker restart with LinkedIn enrichment | ~423 |
| #3931 | 4:57 AM | 🔵 | LLM skill matching failed due to Claude Haiku max_tokens limit, fell back to 0% rule-based matching | ~427 |
| #3902 | 4:39 AM | 🔵 | Job ea71349c enrich_input activity failed 3 times due to LinkedIn Bright Data timeout, succeeded on attempt 4 | ~522 |
| #3897 | 4:35 AM | 🔵 | Worker logs show CancelledError with LinkedIn HTTP error handling context | ~443 |
| #3872 | 4:20 AM | ✅ | PR #95 Merged to Main: Avatar Storage, Skill Matching, and Frontend Fixes | ~386 |
| #3870 | " | 🟣 | Committed avatar file serving, skill matching algorithm improvements, and frontend rebuild | ~511 |
</claude-mem-context>

# Backend

FastAPI + Temporal.io 기반 AI 면접 스크립트 생성 백엔드.

## 디렉토리 구조

```
app/
├── api/routes/     # REST API 엔드포인트
├── core/           # 설정, DB, 보안, 로깅
├── models/         # Pydantic 데이터 모델
├── prompts/        # LLM 프롬프트 YAML
├── services/       # 비즈니스 로직 (LLM 캐시, 라우터)
└── workflows/      # Temporal 워크플로우 + Activity
    └── activities/ # 16개 Activity 함수
```

## 아키텍처 감사 (Issue #77) 적용 현황

| Phase | 내용 | PR |
|-------|------|-----|
| Phase 1 | 보안 강화: 시크릿 검증, Redis 인증, 내부 API 토큰 | #78 |
| Phase 2 | 캐시 키 재설계: Activity 컨텍스트 + LLM_CACHE_ENABLED | #79 |
| Phase 3 | DB 풀 확장, Redis Rate Limiting, 의존성 버전 정리 | #80 |
| Phase 5 | 보안 헤더 미들웨어 (X-Content-Type-Options, X-Frame-Options) | #82 |

## 주요 환경변수 (Phase 1-3 추가분)

```
LLM_CACHE_ENABLED=true       # LLM 캐시 on/off
DB_POOL_SIZE=10               # DB 커넥션 풀
DB_MAX_OVERFLOW=20            # 초과 허용 커넥션
INTERNAL_API_TOKEN=...        # Worker↔Backend 내부 인증
REDIS_PASSWORD=...            # Redis 인증
```

## 최근 변경 (PR #85-87)

| PR | 내용 |
|----|------|
| #85 | 레이더 차트 5번 축 변경 (risk → code_quality), Decision 위험 평가 추가, 모바일 반응형 |
| #86 | 경험 레벨별 퍼센트 기반 질문 배분 시스템 (`max_questions` 제거, `question_distribution` 도입) |
| #87 | 카테고리 가중치 경험 레벨 미적용 버그 수정 (`input_data` flat dict 접근 오류) |

## i18n + Langfuse + 데이터 파이프라인 수정 (5단계)

| Step | 범위 | 수정 파일 |
|------|------|----------|
| 1 | Langfuse SDK v3 호환 | `cached_llm.py` |
| 2 | Finalization 프롬프트 i18n | `finalization.yaml`, `finalization.py` |
| 3 | Rule-based 한국어 → i18n | `i18n_labels.py`(신규), intel/analysis/decision |
| 4 | code_analysis 메타데이터 | `finalization.py` |
| 5 | LinkedIn 필드 방어 | `finalization.py` |

## 개선 사이클 2 (PR #102-106)

| PR | 내용 |
|----|------|
| #102 | 워크플로우 병렬화 + CachedLLMService DRY |
| #103 | OWASP 보안 강화 (5가지) |
| #104 | LLM 출력 품질 강화 |
| #105 | WCAG 탭 접근성 + i18n 전환 |
| #106 | 보안·검증·i18n·성능·DRY 5가지 개선 |

### PR #106 백엔드 주요 변경
- **SSRF 방어**: `finalization.py` 아바타 다운로드 도메인 allowlist
- **Rate Limiting**: POST `/jobs/{id}/retry` 5/min, DELETE `/jobs/{id}` 10/min
- **HSTS**: `main.py` 보안 헤더 추가
- **LLM 검증**: `validate_llm_output()` 헬퍼 (cached_llm.py)
- **DRY**: `match_config.py` 공유 설정 모듈 (intel/analysis 중복 제거)

## 개선 사이클 3 (PR #113, #115)

| PR | 내용 |
|----|------|
| #113 | 테스트 7개 파일 mock 대상 Langfuse-first 패턴 동기화 (474 passed, 4 skipped) |
| #115 | OAuth 로그인 localhost 리다이렉트 수정 — 상대 URL 전환 |

## 테스트

```bash
.venv/bin/python -m pytest tests/ -x --tb=short
```

- **474 passed, 4 skipped** (PR #113 이후 기준)
- PR #113: 7개 테스트 파일에서 mock 대상을 Langfuse-first 패턴(`get_langfuse_prompt`)으로 동기화
- Docker 환경에서는 전체 통과